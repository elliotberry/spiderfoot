services:
  spiderfoot:
    build:
      context: .
      dockerfile: Dockerfile
    image: spiderfoot:local
    ports:
      - "5001:5001"
    volumes:
      - spiderfoot-data:/var/lib/spiderfoot
    restart: unless-stopped

volumes:
  spiderfoot-data:
